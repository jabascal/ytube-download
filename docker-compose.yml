version: "2"
services: 
  ytube:
    build: .
    image: juanabascal/ytube:hydra
    volumes:
    # volume created in /var/lib/docker/volumes/ytube-download_music/_data/
      - music:/usr/src/app/downloads
      - data:/usr/src/app/data
volumes:
  music:
  data:
